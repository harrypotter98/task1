Web development is developing websites for internet or intranet.

Browser is a software application for retrieving, presenting and traversing information resources on the World Wide Web.

When we navigate to a website,the browser makes a request to the server that hosts the website.

Server is a running application of software which accepts request from the browser and responds accordingly.

The World Wide Web (www) is an information system of interlinked hypertext documents that are accessed via the internet.

HTML is Hyper Text Markup Language.It is a mark up language for describing web documents.

CSS is Cascading Style Sheets. It defines how HTML elements are to be displayed. Simplified than HTML.

JavaScript is a high-level programming language which is used to control the browser and change the content in the document that is displayed. It can also change HTML and CSS content.

Domain is a workstation operating system.

 The Internet is a global system of interconnected computer networks that use the standard internet protocol  suite(computer networking model) (TCP/IP) to link several billion devices worldwide.

Internet pack is offer provided in mobiles and computers which allows us to use internet in terms of mb/rupee.

WiFi is a local area wireless computer networking technology that allows electronic devices to network.
 
A uniform resource locator (URL) is a reference to a resource that specifies the location of the resource on a computer network and a mechanism for retrieving it.

A uniform resource identifier(URI)  is used to identify the name of the resource.


